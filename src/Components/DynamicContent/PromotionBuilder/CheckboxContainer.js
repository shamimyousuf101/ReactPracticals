import React from 'react';

import Checkbox from './Checkbox';

const CheckboxContainer = (props) => {

        
        return (
            null
            // <React.Fragment>
            //     {
            //         props.displayItems.map(item => (
            //             <label key = {item.key}>
            //                 {item.name}
            //                 <Checkbox name={item.name} checked={props.value.get(item.name)}/>                           
            //             </label>
            //         ))
            //     }
            // </React.Fragment>

        )
}

export default CheckboxContainer;